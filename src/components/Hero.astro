---
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Dual Pyramid SVG Background (twin ziggurats) -->
  <div class="absolute inset-0 flex items-end justify-center pointer-events-none">
    <svg viewBox="0 0 1400 650" class="w-full max-w-7xl opacity-[0.10]" preserveAspectRatio="xMidYMax meet">
      <defs>
        <linearGradient id="pyramidGlow" x1="0" y1="0" x2="0" y2="1">
          <stop offset="0%" stop-color="#F4C563" stop-opacity="0.9"/>
          <stop offset="100%" stop-color="#D4A84B" stop-opacity="0.15"/>
        </linearGradient>
        <linearGradient id="pyramidFill" x1="0" y1="0" x2="0" y2="1">
          <stop offset="0%" stop-color="#D4A84B" stop-opacity="0.04"/>
          <stop offset="100%" stop-color="#D4A84B" stop-opacity="0.01"/>
        </linearGradient>
      </defs>
      <!-- Primary pyramid (left) -->
      <polygon points="500,80 850,580 150,580" fill="url(#pyramidFill)" stroke="url(#pyramidGlow)" stroke-width="1"/>
      <!-- Secondary pyramid (right, background) -->
      <polygon points="900,140 1200,580 600,580" fill="none" stroke="#D4A84B" stroke-width="0.5" opacity="0.3"/>
      <!-- Ziggurat terrace lines on primary -->
      <line x1="325" y1="330" x2="675" y2="330" stroke="#D4A84B" stroke-width="0.4" opacity="0.25"/>
      <line x1="412" y1="205" x2="588" y2="205" stroke="#D4A84B" stroke-width="0.4" opacity="0.2"/>
      <line x1="237" y1="455" x2="763" y2="455" stroke="#D4A84B" stroke-width="0.3" opacity="0.15"/>
      <!-- Inclinators (exterior elevator tracks) -->
      <line x1="350" y1="500" x2="500" y2="80" stroke="#F4C563" stroke-width="0.3" opacity="0.15"/>
      <line x1="650" y1="500" x2="500" y2="80" stroke="#F4C563" stroke-width="0.3" opacity="0.15"/>
      <!-- Owl eye at apex -->
      <circle cx="500" cy="100" r="8" fill="none" stroke="#F4C563" stroke-width="0.5" opacity="0.4"/>
      <circle cx="500" cy="100" r="3" fill="#F4C563" opacity="0.3"/>
    </svg>
  </div>

  <!-- Ambient amber glow -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/3 w-[800px] h-[400px] bg-amber/5 rounded-full blur-[150px]" style="animation: amber-pulse 8s ease-in-out infinite;"></div>
  </div>

  <!-- Light sweep (BR2049 style horizontal amber drift) -->
  <div class="absolute inset-0 pointer-events-none overflow-hidden">
    <div class="absolute top-0 bottom-0 w-[300px] bg-gradient-to-r from-transparent via-amber/[0.04] to-transparent" style="animation: light-sweep 12s ease-in-out infinite;"></div>
  </div>

  <!-- Water ripple reflections (Wallace Corp interior ceiling reflections) -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-[20%] left-[30%] w-[600px] h-[300px] rounded-full bg-radial-[at_50%_50%] from-amber/[0.06] to-transparent" style="animation: ripple-1 10s ease-in-out infinite;"></div>
    <div class="absolute top-[40%] left-[50%] w-[500px] h-[250px] rounded-full bg-radial-[at_50%_50%] from-amber/[0.04] to-transparent" style="animation: ripple-2 13s ease-in-out infinite;"></div>
  </div>

  <!-- Content -->
  <div class="relative z-10 text-center px-6 max-w-5xl">
    <!-- Owl icon -->
    <div class="mb-6 opacity-0" style="animation: fade-in 1.5s ease forwards 0.1s;">
      <svg viewBox="0 0 40 40" class="w-8 h-8 mx-auto text-amber/40">
        <circle cx="14" cy="18" r="5" fill="none" stroke="currentColor" stroke-width="1"/>
        <circle cx="26" cy="18" r="5" fill="none" stroke="currentColor" stroke-width="1"/>
        <circle cx="14" cy="18" r="2" fill="currentColor"/>
        <circle cx="26" cy="18" r="2" fill="currentColor"/>
        <path d="M9 18 Q14 8 20 12 Q26 8 31 18" fill="none" stroke="currentColor" stroke-width="0.8"/>
        <path d="M17 23 L20 28 L23 23" fill="none" stroke="currentColor" stroke-width="0.8"/>
      </svg>
    </div>

    <p class="font-mono text-xs tracking-[0.4em] text-stone mb-8 opacity-0" style="animation: fade-in 1.5s ease forwards 0.3s;">
      EST. 2019 /// LOS ANGELES /// OFF-WORLD DIVISIONS
    </p>

    <h1 class="font-display text-6xl sm:text-8xl md:text-[10rem] font-light tracking-wide text-ivory leading-[0.85] mb-6 opacity-0" style="animation: fade-up 1.8s cubic-bezier(0.16, 1, 0.3, 1) forwards 0.6s;">
      TYRELL
    </h1>

    <p class="font-mono text-sm tracking-[0.6em] text-stone/60 mb-8 opacity-0" style="animation: fade-in 1.5s ease forwards 0.9s;">
      C O R P O R A T I O N
    </p>

    <p class="font-display text-xl sm:text-2xl italic text-amber font-light mb-12 opacity-0" style="animation: fade-in 1.5s ease forwards 1.2s;">
      "More human than human."
    </p>

    <div class="flex items-center justify-center gap-6 sm:gap-10 font-mono text-[10px] sm:text-xs tracking-[0.3em] text-stone/60 opacity-0" style="animation: fade-in 1.5s ease forwards 1.6s;">
      <span>NEXUS DIVISION</span>
      <span class="text-amber/30">&bull;</span>
      <span>GENETIC DESIGN</span>
      <span class="text-amber/30">&bull;</span>
      <span>MEMORY SYSTEMS</span>
      <span class="text-amber/30">&bull;</span>
      <span>OFF-WORLD OPS</span>
    </div>
  </div>

  <!-- Bottom grid line -->
  <div class="absolute bottom-0 left-0 right-0 grid-line"></div>
</section>
